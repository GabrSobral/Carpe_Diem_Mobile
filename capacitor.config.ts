import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'ionic_carpediem',
  webDir: 'build',
  bundledWebRuntime: false,
};

export default config;
